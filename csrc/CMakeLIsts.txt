set(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/../bin)

set(SOURCES display)

add_library(csrc ${SOURCES})

target_link_libraries(csrc)

set_target_properties(csrc PROPERTIES OUTPUT_NAME "richman_csrc")

